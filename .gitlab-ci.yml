include:
  - project: 'shared/gitlab-ci-includes'
    ref: main
    file:
      - '/base.yml'
      - '/debian-packages.yml'
      - '/python.yml'

variables:
  APTLY_BASE_REPOSITORY: cluster

stages:
  - test
  - build
  - publish

unit test and feature test:
  stage: test
  image: python:3.11
  services:
    # https://gitlab.com/gitlab-org/gitlab-runner/-/issues/3163#note_63471739
    - name: rabbitmq:3.12.12-management
      alias: rabbitmq
  variables:
    RABBITMQ_DEFAULT_USER: test
    RABBITMQ_DEFAULT_PASS: 'knvPSBPLkJSwTdMn6F4kkQkB'
    RABBITMQ_DEFAULT_VHOST: ci-test
  script:
    - !reference [.add repositories, script]
    - apt-get -y install python3-cyberfusion-common python3-sdnotify python3-yaml python3-systemd python3-pika python3-cryptography python3-pydantic python3-cached-property python3-docopt python3-schema
    - pip install --no-cache-dir -r requirements/test.txt
    - unlink rabbitmq.yml
    - ln -s rabbitmq.ci.yml rabbitmq.yml
    - PYTHONPATH=$CI_PROJECT_DIR/src:/usr/lib/python3/dist-packages pytest tests/RabbitMQConsumer/ -vvv --cov-branch --cov=cyberfusion.RabbitMQConsumer --cov-config=.coveragerc --cov-fail-under=1 --junitxml=report-pytest.xml
  artifacts:
    reports:
      junit: report-*.xml
