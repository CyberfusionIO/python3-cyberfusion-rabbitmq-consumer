---
server:
  host: localhost
  user: guest
  password: guest
  port: 5671
  ssl: true

virtual_hosts:
  virtual_host1:
    exchanges:
      dx_configuration_manager_present:
        routing_keys:
          - Admin

exchanges:
  dx_configuration_manager_present:
    command: tuxis-cron configuration-manager-present.cron
